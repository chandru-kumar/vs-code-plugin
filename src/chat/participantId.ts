/**
 * Single canonical place for the chat participant id.
 * Imported by participant.ts and contextBuilder.ts to avoid a circular
 * dependency between them.
 */
export const PARTICIPANT_ID = 'pilotcode.chat';
